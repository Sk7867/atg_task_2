gsap.registerPlugin(ScrollTrigger);

gsap.from('.svg_anim1, .anim1, .btn_anim1', {
  scrollTrigger: {
    trigger: '#section_1',
    start: 'center bottom',
    end: 'top bottom',
    // markers: true,
    toggleActions: 'restart none none reverse',
  },
  y: '50%',
  opacity: 0,
  duration: 0.7,
  stagger: 0.2,
  opacity: 0,
  ease: 'power3.in',
});

gsap.from('.svg_anim2, .anim2, .btn_anim2', {
  scrollTrigger: {
    trigger: '#section_2',
    start: 'center bottom',
    end: 'top bottom',
    // markers: true,
    toggleActions: 'restart none none reverse',
  },
  y: '50%',
  opacity: 0,
  duration: 0.7,
  stagger: 0.2,
  opacity: 0,
  ease: 'power3.in',
});

gsap.from('.svg_anim3, .anim3, .btn_anim3', {
  scrollTrigger: {
    trigger: '#section_3',
    start: 'center bottom',
    end: 'top bottom',
    // markers: true,
    toggleActions: 'restart none none reverse',
  },
  y: '50%',
  opacity: 0,
  duration: 0.7,
  stagger: 0.2,
  opacity: 0,
  ease: 'power3.in',
});

gsap.from('.svg_anim4, .anim4, .btn_anim4', {
  scrollTrigger: {
    trigger: '#section_4',
    start: 'center bottom',
    end: 'top bottom',
    // markers: true,
    toggleActions: 'restart none none reverse',
  },
  y: '50%',
  opacity: 0,
  duration: 0.7,
  stagger: 0.2,
  opacity: 0,
  ease: 'power3.in',
});

gsap.from('.svg_anim5, .anim5, .btn_anim5', {
  scrollTrigger: {
    trigger: '#section_5',
    start: 'center bottom',
    end: 'top bottom',
    // markers: true,
    toggleActions: 'restart none none reverse',
  },
  y: '50%',
  opacity: 0,
  duration: 0.7,
  stagger: 0.2,
  opacity: 0,
  ease: 'power3.in',
});

gsap.from('.svg_anim6, .anim6, .btn_anim6', {
  scrollTrigger: {
    trigger: '#section_6',
    start: 'center bottom',
    end: 'top bottom',
    // markers: true,
    toggleActions: 'restart none none reverse',
  },
  y: '50%',
  opacity: 0,
  duration: 0.7,
  stagger: 0.2,
  opacity: 0,
  ease: 'power3.in',
});

gsap.from('.svg_anim7, .anim7, .btn_anim7', {
  scrollTrigger: {
    trigger: '#section_7',
    start: 'center bottom',
    end: 'top bottom',
    // markers: true,
    toggleActions: 'restart none none reverse',
  },
  y: '50%',
  opacity: 0,
  duration: 0.7,
  stagger: 0.2,
  opacity: 0,
  ease: 'power3.in',
});
